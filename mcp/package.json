{
  "name": "tollroad-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "TollRoad vibe-DJ MCP server — exposes TollRoad HTTP API as MCP tools for AI agents.",
  "type": "module",
  "bin": {
    "tollroad-mcp": "./src/server.ts"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types --test src/client.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
